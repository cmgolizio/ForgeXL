"""The backend's data models.

``schemas`` holds the Pydantic models describing API-facing structured data.
``run`` holds the logical Run — runtime state, deliberately not Pydantic
(build plan 6B.1).
"""