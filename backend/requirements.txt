# Local Data Workbench — backend dependencies
#
# Direct dependencies only, pinned to the versions resolved at implementation
# time (Phase 1.3). Transitive dependencies are resolved by pip.
#
# Install into the project virtual environment:
#
#     python3 -m venv backend/.venv
#     backend/.venv/bin/python -m pip install --upgrade pip
#     backend/.venv/bin/python -m pip install -r backend/requirements.txt

# HTTP API
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# Data engine
polars==1.43.2

# Excel input (preferred engine) and compatibility fallback
fastexcel==0.21.0
openpyxl==3.1.5

# Excel output
xlsxwriter==3.2.9

# Tests
pytest==9.1.1
httpx==0.28.1